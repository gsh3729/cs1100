#include <stdio.h>
int main()
{
 int a,b,c;
 printf("enter the numbers:");
 scanf("%d%d%d",&a,&b,&c);
 if(a>=b)
  {
   if(b>=c)
   printf("descending");
  }
 else 
  {
   if(b<=c)
   printf("ascending");
   else
   printf("neither");
  }
return 0;
}

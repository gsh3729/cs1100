/*
 program name:average.c
 program description : the program takes two integers as input
 and displays their average.
 programmer's name:sriharsha\
 date:09-08-2016
*/

#include <stdio.h>
int main()
{ 
/*x and y are two variables to store the two inputs*/
int x,y;
/* z is the variable to store the average of x and y*/
int z;
/*prompt the user to enter the first number and read it*/
printf("enter the number:");
scanf("%d",&x);
/*prompt the user to enter the second number and read it*/
printf("enter the second number:");
scanf("%d",&y);
/*compute the average*/
z=(x+y)/2;
/*display the result */
printf("the average of %d and %d is %d.\n",x,y,z);
/*tell the operating system that every thing went well*/
return 0;
}

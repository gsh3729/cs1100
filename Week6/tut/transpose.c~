#include<stdio.h>
int main()
{
 int i, j, a[10][10], n, t;
 printf("Give dimension (<=10) \n");
 scanf("%d", &n);
 if(n<=10)
 {   
  printf("enter elements of a %d X %d matrix\n", n, n);
  for(i=0; i<n; i++)
  {
   for(j=0; j<n; j++)
   {
    scanf("%d", &a[i][j]);
   }
  }
  for(i=0; i<n; i++)
  {
   for(j=0; j<n; j++)
   {
    
   }
  }
  printf("transpose is \n");
  for(i=0; i<n; i++)
  {
   for(j=0; j<n; j++)
   {
    printf("%2d ", a[i][j]);
   }
  }
 }
}

#include <stdio.h>
int main()
{
 int a,b,i=1,j,mul;
 printf("enter the numbers:");
 scanf("%d%d",&a,&b);
 while(i<=a)
 {
  j=1;
   while(j<=b)
   {
    mul=i*j;
    if(j<b)
    printf("%4d",mul);
    else
    printf("%4d\n",mul);
    j++;
   }
  i++;
 }
}

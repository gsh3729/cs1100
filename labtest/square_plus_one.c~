/*NAME: G SRIHARSHA 
  DATE: 20/09/2016*/
#include <stdio.h>
int main()
{
      int n,k,j;

      printf("enter the number n:");
      scanf("%d",&n);
     /* reading the value of numbern*/
      k=0;
      j=0;
     /* intializing the variables k,j as 0 */
      while(k<n)/* using a while loop we are trying to check the condition*/
      {
  
       if(n==k*k+1)
       {
        printf("YES\n");
        j++;
        break;
       }

       k++;
      } 
  
      if(j==0)
      {
       printf("NO\n");
      }/* if the variable is not increased that means the number is not satisfying the conditon*/
     
      return 0;
}
